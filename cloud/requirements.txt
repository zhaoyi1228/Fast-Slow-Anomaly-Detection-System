# Cloud Side Requirements
# 云侧依赖 - 服务器端Agent服务

# API 服务
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
python-multipart>=0.0.6

# 临时帧落盘 / 文件处理
aiofiles>=23.0.0

# 图像处理
opencv-python>=4.5.0
numpy>=1.20.0

# HTTP通信 / 配置
requests>=2.28.0
pyyaml>=6.0

# 可选：本地补回归测试时使用
pytest>=7.0.0

# 说明：cloud 还依赖外部 AnomalyAgent 项目本身的依赖，
# 需额外安装：pip install -r AnomalyAgent/requirements.txt