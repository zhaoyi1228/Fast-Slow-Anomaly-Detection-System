# Edge Side Requirements
# 端侧依赖 - 机器狗端

# 推理与数值计算
torch>=1.10.0
numpy>=1.20.0
opencv-python>=4.5.0

# 服务与通信
flask>=2.0.0
requests>=2.28.0

# 配置/工具
pyyaml>=6.0

# RealSense 相机（仅真实端侧机器狗环境需要）
pyrealsense2>=2.50.0; platform_system != "Darwin"

# 可选：本地补回归测试时使用
pytest>=7.0.0